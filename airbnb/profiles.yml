airbnb:
  outputs:
    dev:
      type: snowflake
      account: DVMZFSM-KC22031
      user: dbt

      role: TRANSFORM
      private_key_path: "{{ env_var('DBT_SNOWFLAKE_PRIVATE_KEY_PATH') }}"
      private_key_passphrase: "{{ env_var('DBT_ENV_SECRET_SNOWFLAKE_PRIVATE_KEY_PASSPHRASE') }}"

      database: AIRBNB
      schema: DEV
      threads: 6
      warehouse: COMPUTE_WH
  target: dev
